task wrapper(type: Wrapper) {
    gradleVersion = '3.1'
}

allprojects {
    repositories {
        jcenter();
    }
    apply plugin: 'java'
    sourceCompatibility = '1.8'
    configurations.all {
        resolutionStrategy {
            failOnVersionConflict()
            // MIT
            force 'org.slf4j:slf4j-api:1.7.20'
        }
    }
    apply plugin: 'idea'
    apply plugin: 'java-library-distribution'
}

project('draw-awt') {
    dependencies {
        compile project(':draw')
    }
}

project('gui') {
}

project('gui-awt') {
    dependencies {
        compile project(':gui')
    }
}

project('music-notation-editor') {
    dependencies {
        compile project(':draw')
        compile project(':gui')
        compile project(':music-notation')
        compile project(':unclear')
        // LGPL 3.0
        compile 'org.audiveris:proxymusic:3.0.1'
    }
}

project('music-notation-editor-swing') {
    dependencies {
        compile project(':draw-awt')
        compile project(':gui-awt')
        compile project(':music-notation-editor')
        // LGPL 2.1
        compile 'ch.qos.logback:logback-classic:1.1.7'
    }
}

dependencies {
    compile project('music-notation-editor-swing')
}
